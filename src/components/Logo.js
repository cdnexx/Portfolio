const styles = {
    logo: {
        fontWeight: '700',
        fontSize: '2rem'
    }
}

const Logo = () => {
    return(
        <div style={styles.logo}>
            Franco Osorio
        </div>
    )
}

export default Logo